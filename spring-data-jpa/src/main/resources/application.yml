# 应用名称
spring:
  application:
    name: spring-data-jpa
  # 数据库配置
  datasource:
    url: jdbc:mysql://192.168.101.100:3306/teach?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    #格式化SQL
    properties:
      hibernate:
        formart-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

#JPA配置

server:
  port: 8080

